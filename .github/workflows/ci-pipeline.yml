name: .NET MVC CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release

      - name: Run tests
        run: dotnet test --no-build --logger:"junit;LogFilePath=test-results.xml"

      - name: Upload test report
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results.xml

      # Notificación por correo si falla
      - name: Send email with CI results
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: "hermestraducciones844@gmail.com"
          password: "rkqazagvvxaxjtwh"
          subject: "❌ CI Pipeline Failed - .NET MVC"
          body: "El flujo de trabajo de CI de tu proyecto .NET MVC finalizo con el estado ${{ job.status }}. Consulta los registros de GitHub Actions: ${{ github.run_id }}"
          to: "joelpinedabeltre515@gmail.com"
          from: "hermestraducciones844@gmail.com"
          secure: false
          attachments: ./test-results.xml
